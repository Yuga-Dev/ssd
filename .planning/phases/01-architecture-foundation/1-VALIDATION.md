# Phase 1 Validation Strategy
**Dimension 8: Verification Goals**

- `PWA installable`: Lighthouse checks or verifying manifest existence.
- `Socket connection`: Client connects to `ws://localhost:3001` and fires connect event.
- `Appwrite connection`: SDK is initialized with endpoint and project ID.
