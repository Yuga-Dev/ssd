# Phase 5 Validation Strategy

**Verification Goals:**
- Executing the provisioning script establishes the missing Appwrite database and schema attributes automatically without failing on pre-existing structures.
- Host Lobby UI renders a new time-configuration interactive component (e.g., 3 minutes).
- Active Session Timers accurately display the selected configuration limit correctly mapping the relative `endTime` payloads.
- End-game state remains functional identically.
